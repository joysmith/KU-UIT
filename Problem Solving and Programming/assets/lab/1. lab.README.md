# Turbo ++

```c++
/*******************************************
Write a C++ program to demonstrate
a. Basic input/output, arithmetic operations.
Programmer - Tanishka Prabhu, CSE-Cloud, 1 Sem
Written For - Karnavati University
********************************************/


#include <iostream.h>
#include <conio.h>	//required for turbo

void main()
{
    clrscr();		//required for, turbo clears screen
    int a;
    int b;
    int sum;
    int mul;
    int sub;
    int division;

    cout << "enter two numbers ";
    cin >> a >> b;//taking in values

    sum = a+b;     //calculating sum
    sub = a-b;    //subtraction
    mul = a*b;    //multiplication

    //displaying values
    cout<<"sum= "<<sum<<endl;
    cout<<"subtraction = "<<sub<<endl;
    cout<<"multiplication = "<<mul<<endl;

    //condition to make sure that denominator is not zero
    if(b==0)
    {
        cout << "division not possible";
    }
    else
    {
        division = a/b;
        cout << "division = "<<division;
    }

    getch();// turbo specific
    // is like a "pause" button for the program.
    //It makes the program wait until you press any key on your keyboard.

}

```

<br>
<br>

# VS Code

```c++
/*******************************************
Write a C++ program to demonstrate
a. Basic input/output, arithmetic operations.
Programmer - Tanishka Prabhu, CSE-Cloud, 1 Sem
Written For - Karnavati University
********************************************/

#include <iostream>
using namespace std;

int main()
{

    int a;
    int b;
    int sum;
    int mul;
    int sub;
    int division;

    cout<<"enter two numbers ";
    cin>>a>>b;//taking in values

    sum=a+b;     //calculating sum
    sub=a-b;    //subtraction
    mul=a*b;    //multiplication

    //displaying values
    cout<<"sum= "<<sum<<endl;
    cout<<"subtraction = "<<sub<<endl;
    cout<<"multiplication = "<<mul<<endl;

    //condition to make sure that denominator is not zero
    if(b==0)
    {
        cout<<"division not possible";
    }
    else
    {
        division=a/b;
        cout<<"division = "<<division;
    }
   return 0;
}

```
