# Turbo ++ by Neil Thakkar, CSE-Cloud, 1 Sem

```c++
/*******************************************
Write a C++ program to demonstrate
b. The largest of 3 numbers.
Programmer - Neil Thakkar, CSE-Cloud, 1 Sem
Written For - Karnavati University
********************************************/


#include<iostream.h>
#include<conio.h>//this library need to be use in turbo for input and output

int main()
{

	clrscr();

	// Here we have declared three integer variables a, b, and c to store the input numbers.
	int a,b,c;
	cout<<"Enter three numbers: "<<endl;

	// We read the three numbers from the user.
	cin>>a>>b>>c;

	if(a>b && a>c)// && is a logical AND operator that checks if both conditions are true.
	{
		cout<<a<<" is the largest number";
	}
	// else if is used to check the next condition if the previous one is false.
	else if(b>c)
	{
		cout<<b<<" is the largest number";
	}
	else
	{
		cout<<c<<" is the largest number";
	}
	getch();
	return 0;
}


/* output
Enter three numbers:
11 22 9
The largest number is: 22
*/

```

<br>
<br>

# VS Code by Neil Thakkar, CSE-Cloud, 1 Sem

```c++
/*******************************************
Write a C++ program to demonstrate
b. The largest of 3 numbers.
Programmer - Neil Thakkar, CSE-Cloud, 1 Sem
Written For - Karnavati University
********************************************/

#include<iostream>
using namespace std;

int main()
{

    // Here we have declared three integer variables a, b, and c to store the input numbers.
    int a, b, c;

    cout << "Enter three numbers: "<<endl;

    // We read the three numbers from the user.
    cin >> a >> b >> c;

    if (a > b && a > c)// && is a logical AND operator that checks if both conditions are true.
    {
        cout << "The largest number is: " << a;
    }
    // else if is used to check the next condition if the previous one is false.
    else if (b > c)
    {
        cout << "The largest number is: " << b;
    }
    else
    {
            cout << "The largest number is: " << c;
    }
    return 0;
}


/* output
Enter three numbers:
11 22 9
The largest number is: 22
*/

```
